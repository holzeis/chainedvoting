{
  "name": "chained-voting",
  "version": "0.0.1",
  "description": "Webapplication built on top of Hyperledger Fabric",
  "main": "docker-compose up",
  "os": [
    "darwin",
    "linux"
  ],
  "engines": {
    "npm": "~3.10.8",
    "node": "~6.9.1"
  },
  "scripts": {
    "start": "docker-compose down && VERSION=$(date +%Y%m%d%H%M%s) docker-compose up --build --force-recreate",
    "install": "npm run getbaseimage && docker-compose pull && docker-compose build",
    "getbaseimage": "docker pull hyperledger/fabric-ccenv:x86_64-1.0.0-beta && docker tag hyperledger/fabric-ccenv:x86_64-1.0.0-beta",
    "gobuild": "cd src/chained-voting && go build chaincode.go",
    "test-go": "cd src/chained-voting && go test $(ls -d1 ./*/ | grep -v vendor | tr \"\n\" \" \" ) ./",
    "test": "npm -s run test-go",
    "cf": "docker build --tag cf . && docker run -it --rm -v $(pwd)/server/resources:/usr/src/app/server/resources cf"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@git.ng.bluemix.net:gbs_dach_blockchain/chained_voting.git"
  },
  "keywords": [
    "hyperledger",
    "fabric",
    "nodejs",
    "angular",
    "typescript",
    "blockchain"
  ],
  "author": "IBM GBS DACH Blockchain Task Force",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://git.ng.bluemix.net/gbs_dach_blockchain/chained_voting/issues"
  },
  "homepage": "https://git.ng.bluemix.net/gbs_dach_blockchain/chained_voting"
}
